import { LoginForm } from "@/components/auth/login-form";

export default function BuyerLoginPage() {
  return (
    <LoginForm
      mainTitle="Agroledger Marketplace"
      formTitle="Login to Marketplace"
      role="buyer"
    />
  );
}
